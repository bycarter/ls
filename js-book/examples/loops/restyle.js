let iceCreamTaste = 'chocolate'; // use camelCase and end declarations with ';'
let iceCreamDensity = 10;

while (iceCreamDensity > 0) { // open block curly brace on same line
    console.log('Drip...');
    iceCreamDensity -= 1;
}

console.log(`The ${iceCreamTaste} ice cream melted.`); // use str interpolation
