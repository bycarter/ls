// The data is fine.  Object.values() returns a new array holding the values from
// the object demoObject, and then assigns values to that new array.  It does not
// access the passed in object's attributes
//
// WRONGGG
